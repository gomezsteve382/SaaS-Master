/* AUTO-GENERATED by tools/asset-sweep — DO NOT EDIT. */
/* Re-run with `pnpm sweep:assets`. */
/* Source-of-truth: tools/asset-sweep/src/ports.mjs (hand-ported
   from the canonical Python in srtlab_canflash_algos.py). */

/**
 * Extended seed-key catalog: algorithm primitives discovered in
 * attached_assets/ that are NOT already implemented in algos.js,
 * canflashAlgos.js, or alfaobdAlgorithms.generated.js.
 *
 * Each entry exposes:
 *   - `tag`            — canonical, snake-case identifier
 *   - `label`          — human-readable name for the SeedTab picker
 *   - `params`         — original Python signature
 *   - `docstring`      — Python docstring (first line)
 *   - `signatures`     — magic-constant signature tags from the sweep
 *   - `sourcePaths`    — every path in attached_assets/ where it was
 *                        detected (asset corpus dedup'd to virt paths)
 *   - `coverageStatus` — one of `"already-implemented"` /
 *                        `"partial-match"` / `"new"` (per the
 *                        unlock-coverage contract). Hand-ported
 *                        entries are typically `"new"` and carry
 *                        `ported: true` so the UI can flag them.
 *   - `ported`         — `true` for entries below (the file's
 *                        whole reason for existence is to ship
 *                        executable JS for them).
 *   - `vectors`        — pinned (seed, key) pairs verifying `fn`
 *   - `fn(seedU32)`    — JavaScript port, returns key as u32 number
 *
 * SeedTab merges `EXTENDED_FN_BY_TAG` into its dispatcher fallback so
 * any tag here is selectable in the picker; `tools/asset-sweep` re-
 * verifies every `vectors` entry on every run and refuses to emit if
 * any port drifts.
 */

export const EXTENDED_ALGORITHMS = [
];

export const EXTENDED_ALGORITHMS_COUNT = EXTENDED_ALGORITHMS.length;

/**
 * tag → fn(seedU32) lookup, used by SeedTab to extend its picker
 * without modifying the curated `ALGOS` list in algos.js.
 */
export const EXTENDED_FN_BY_TAG = Object.freeze(
  Object.fromEntries(EXTENDED_ALGORITHMS.map((a) => [a.tag, a.fn]))
);

/**
 * Vector-verification self-test result captured at sweep time.
 * Every `failed` array is empty when this file is committed —
 * `pnpm sweep:assets` refuses to emit otherwise.
 */
export const EXTENDED_VERIFICATION = [];
