/**
 * vault.ts — Legacy stub kept for compatibility.
 * All vault data is now stored in the database (analysis_results table).
 * The vault array is no longer used — see server/index.ts for DB-backed endpoints.
 */

export const vault: any[] = [];
